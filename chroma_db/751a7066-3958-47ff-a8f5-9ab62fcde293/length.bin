# AI Agent Security and Input Injection Defense

Large language model (LLM)–based agents are vulnerable to a variety of prompt and input injection attacks. These attacks attempt to manipulate the model’s behavior by inserting malicious text inside user inputs, retrieved documents, or even system messages. The goal is often to override safety rules, exfiltrate secrets, or cause unintended tool 